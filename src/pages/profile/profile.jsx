import React from 'react';

import ProfileContainer from "../../componens/profileContainer/profileContainer";

const Profile = () => (
   <div className="page-container">
      <ProfileContainer/>
   </div>
);

export default Profile;
